# NuGet deployment guidance (zcap-dotnet)

## Plan

- [x] Inspect release workflows and package metadata.
- [x] Extract exact release triggers, required secrets, and versioning rules.
- [x] Provide a concise step-by-step deployment guide for core and ASP.NET packages.
- [x] Capture correction lesson in tasks/lessons.md.

## Notes

- NuGet release workflows already exist:
  - `.github/workflows/release-core-nuget.yml` (tag: `core-v*.*.*`)
  - `.github/workflows/release-aspnet-nuget.yml` (tag: `aspnet-v*.*.*`)
- Package version source is `Directory.Build.props` (`ZcapLdVersion`), currently `0.1.2`.
- Required GitHub secrets:
  - `NUGET_API_KEY_CORE` (fallback `NUGET_API_KEY`)
  - `NUGET_API_KEY_ASPNET` (fallback `NUGET_API_KEY`)
- Runbook exists at `docs/NUGET-RELEASE.md`.

## Review

- Deployment guidance can use either:
  - tag-driven release triggers (recommended), or
  - manual `workflow_dispatch` with a `version` input.
- No code/workflow edits required to support NuGet deployment in current state.
